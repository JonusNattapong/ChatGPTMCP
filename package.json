{
  "name": "chatgpt-machine-mcp",
  "version": "0.2.0",
  "description": "Local MCP bridge that lets ChatGPT Web operate a machine and delegate work to Codex CLI",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "chatgpt-machine-mcp": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "npm run build && node --test dist/*.test.js",
    "prepare": "npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
